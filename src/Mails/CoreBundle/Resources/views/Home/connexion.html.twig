<div class="navbar-right">
  {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
      Connecté en tant que <strong>{{ app.user.username | title }}</strong> - <a class="souligne" href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
  {% else %}
     <a class="souligne" href="{{ path('fos_user_security_login') }}">Connexion</a>
  {% endif %}
</div>
